package src.main.java.banking_system;

import java.util.Date;

public class Transaction implements Comparable<Transaction> {
    // Define attributes: timestamp, sourceAccount, destinationAccount, amount, ect.

    // Constructor

    // Implement methods for transaction management

    // Override compareTo method to sort transactions by timestamp
    @Override
    public int compareTo(Transaction other) {
        // Implement comparison logic based on timestamp
        return 0;
    }
}
